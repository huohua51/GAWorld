\section{Execution Architecture}
GAWorld currently operates through a compatibility-preserving microkernel skeleton, event dispatch,
plugin lifecycle, and configurable cognition pipeline while major domain modules remain on direct or
hook-based integration paths.
