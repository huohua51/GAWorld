# Development extras. Install with:
#   pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt

# Test runner
pytest>=7.0
pytest-cov>=4.0

# Lint / format / type
ruff>=0.4
black>=24.0
mypy>=1.8
